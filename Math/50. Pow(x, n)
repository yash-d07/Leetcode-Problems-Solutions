class Solution {
public:
    double myPow(double x, int n) {
        double po = 0;
        if(n==0)
            return 1;
        else
        {
            po = pow(x,n);
        }
        return po;
    }
};
